Finding Key Connnectors using friendship data. This results in capturing a network metric: Degree Centrality.  

**Degree Centrality:** measures how many connections a node has to other nodes in a network. 